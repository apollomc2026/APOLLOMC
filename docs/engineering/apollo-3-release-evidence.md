# APOLLO 3 internal release evidence

Authoritative product direction: `guiding light/Apollo guiding light.docx` in the APOLLO workspace.

## Product spine

1. A user begins with a natural-language outcome in New Mission. Mission Control is the portfolio command deck and exposes one-click reflight plus immediate environment actions; Telemetry owns granular delivered-flight review, revision configuration, and version lineage.
2. APOLLO separates stated facts, evidence-derived facts, and inference; recommends one specialist playbook; and asks one consequential question.
3. Every conversational or evidence change creates a durable specification version. Readiness is capped below approval until the selected document module has all required inputs.
4. Approval atomically locks the current specification. The compiler refuses missing fields instead of fabricating content.
5. The canonical executor retrieves hash-bound evidence, generates a schema-constrained document, applies deterministic checks, renders a PDF draft, records checkpoints, and writes final custody to Google Drive.
6. A delivered draft accepts presentation-only natural-language revision instructions as a new idempotent job. The instruction is hash-bound, the prior draft remains intact, and any requested fact, figure, evidence, brand, or deliverable-identity change must return through Edit Mission Data and a fresh approval.

## Requirement evidence

| Guiding-light requirement | Implementation evidence | Automated gate |
|---|---|---|
| Natural-language first | `components/mission-control/MissionControl.tsx`, `/api/mission-control/interpret` | `tests/mission-control.spec.ts` |
| Durable voice parity | Browser speech recognition streams interim text into the same editable mission draft; low-confidence and critical values require explicit review before submission. Voice origin, confidence, and review confirmation are persisted atomically with the turn and remain visible in the restored transcript | `tests/mission-control.spec.ts`, `supabase/tests/apollo_mission_control_rls_test.sql` |
| Versioned semantic control plane | `lib/mission-control/contracts.ts`, `repository.ts`, migration RPCs | Typecheck and RLS pgTAP suite |
| Fact provenance and evidence custody | `lib/mission-control/evidence.ts`, evidence API, SHA-256 source manifests | `tests/mission-evidence.test.ts` |
| Evidence conflict adjudication | Contradictory source values remain an explicit blocking conflict with every candidate preserved; a direct operator answer adjudicates the conflict, while later contradictory evidence truthfully reopens it | `tests/mission-evidence.test.ts` |
| Complete fact and execution lineage | Every fact carries normalized value, source reference, capture method, confidence, verification state, sensitivity, editor and timestamp; every work order traces the approved specification hash, playbook, model versions, checks and accepted unresolved items | `tests/mission-interpreter.test.ts`, `tests/specification-work-order.test.ts` |
| Specialist recommendation and current model routing | Deterministic specialist routing is augmented by Claude Sonnet 5. All interpretation, evidence extraction, structured generation, repair, and section-drafting calls resolve through one role-aware registry; the default `claude-sonnet-5` identifier was revalidated against Anthropic's active-model catalog on 2026-09-14 | `tests/mission-interpreter.test.ts`, `tests/golden-missions.test.ts`, `tests/model-selection.test.ts` |
| Complete catalog reachability | Every one of the 37 active deliverables is recognizable by its catalog name through conversational intake, loads its specialist module and schema, and compiles a complete approved fact set into a contract-valid work order | `tests/catalog-compile.test.ts` |
| Complete catalog rendering | Every one of the 37 active deliverables passes through its production layout with required body sections, controlled HTML, custom-client identity, cross-brand isolation, and exactly one renderer-owned cover for packet/package/closeout artifacts | `tests/render-matrix.test.ts` |
| Truthful approval gate | `executionGaps`, atomic approval RPC, immutable content hash | `tests/specification-work-order.test.ts` |
| Recoverable approved execution | An already-locked specification can idempotently retry execution after an external dependency such as Drive is restored, without reopening or mutating approval | `tests/system-surfaces.spec.ts` |
| Recoverable blocked workflow | A mid-workflow dependency failure remains immutable while a resolved dependency starts a deterministic child retry with explicit `retry_of` and attempt lineage; retry and revision rebind only the approved evidence manifest to fresh URLs and reject hash drift | `tests/specification-work-order.test.ts`, `tests/evidence-refresh.test.ts`, `tests/system-surfaces.spec.ts` |
| Canonical durable execution | `lib/executor/accept.ts`, `workflows/document-job.ts` | executor contract and financial verification tests |
| Enforced execution lifecycle | Application and Postgres guards reject skipped, backward and post-terminal document-job transitions while retaining retry-safe idempotent checkpoints | `tests/job-state-machine.test.ts` plus live trigger verification |
| Reconnectable Drive delivery | Per-user OAuth connection in Settings or directly from an approved/blocked mission; signed, expiry-bound OAuth state returns the operator to the exact durable mission and rejects external redirects; refresh credentials are encrypted with AES-256-GCM and readable only through the service role; revoked grants stop at a truthful `blocked` checkpoint | `tests/google-drive-auth.test.ts`, `tests/google-drive-custody.test.ts`, `tests/system-surfaces.spec.ts` |
| Truthful custody capability | Public executor capabilities distinguish configured Drive infrastructure from per-user delivery readiness and never report custody ready without user context | `tests/capabilities-contract.test.ts` plus hosted capability probe |
| Content-first workmanship floor | Nine user-supplied field, commercial, QC, and contract-review PDFs were visually inspected across all 51 pages and distilled into `docs/engineering/apollo-deliverable-quality-bar.md`; generation performs a deterministic archetype audit and one fact-preserving repair before decoration | `tests/deliverable-quality.test.ts` plus rendered field-record QA fixture |
| Agreement factual integrity | Contract packages must retain the exact approved parties, governing jurisdiction, and term after generation; omission or mutation fails before rendering or Drive delivery | `tests/agreement-verification.test.ts` |
| Federal response identity integrity | Federal proposals must retain the exact approved solicitation, agency, offeror, NAICS, and set-aside identity; mutation or omission fails before rendering or Drive delivery | `tests/federal-verification.test.ts` |
| Field-record factual integrity | Daily construction and final-QC reports must retain approved project identity plus every crew, activity, work-status, acceptance-criteria, and measured-result row; mutation or omission fails before decoration or Drive delivery | `tests/field-record-verification.test.ts` |
| Commercial factual integrity | Quotes and invoices retain every approved line-item row; change orders also retain cost rows and prove the prior and revised contract-sum arithmetic before delivery | `tests/commercial-verification.test.ts` |
| Financial-class verification | All eight finance/accounting formats have a deterministic verification path. Cash-flow packages cross-foot balances and continuity; statements preserve practitioner figures verbatim; the other six formats preserve every approved numerical source value, and tax estimates require a planning-only professional boundary | `tests/financial-verification.test.ts` |
| Terminal notification routing | Completion email links open the Telemetry-owned mission record; failure email links open New Mission calibration directly, without leaking mission work back onto the dashboard | `tests/mission-email-routing.test.ts` |
| Terminal notification recovery | Immediate workflow notifications use atomic per-job claims. An authenticated ten-minute reconciliation and an owner-scoped post-dashboard pass recover future unsent terminal alerts without duplicate sends; a fixed 2026-09-14 cutoff excludes historical pilot failures from an accidental email flood | `tests/notification-reconciler.test.ts`, `tests/notification-cron.test.ts` |
| Review by instruction | `/api/mission-control/revise` preserves the prior job, increments immutable artifact versioning, and binds the normalized presentation-only directive by SHA-256. The executor rejects a missing, altered, or falsely scoped directive and locks facts, figures, commercial terms, evidence, brand, and deliverable identity to the approved specification; blocked runs can be recovered inside the review workbench and non-artifact attempts are not mislabeled as drafts | idempotent revision, constrained-directive, revision-authority, and browser review tests |
| Final-PDF factual proof | Every specialist verifier is rerun against text parsed from the exact rendered PDF bytes before custody upload. The manifest retains PDF metrics, a normalized factual-content SHA-256, and the `specialist-pdf-v1` verification profile; pilot readiness rejects artifacts without that proof | `tests/pdf-integrity.test.ts`, specialist verification suites, and pilot-readiness tests |
| Verified APOLLO pickup | Operator-facing links never expose private Drive URLs. Pickup checks ownership, exact PDF byte SHA-256, MIME and signature; newly generated artifacts are reparsed at pickup and their normalized text must match the final-PDF factual digest before APOLLO serves them. Legacy artifacts retain byte-integrity access while new pilot evidence is held to the stronger profile | `tests/artifact-access.test.ts` and the authenticated artifact route |
| Aerospace-industrial interface | Mission Control component and global design system | desktop/mobile browser tests |
| Billing dormant | internal-only billing configuration | billing scaffold tests |
| Browser privacy | service worker caches only immutable public assets | `tests/pwa-privacy.test.ts` |
| Authorized legacy previews | Preview URLs are minted only after a session-scoped output-ID lookup protected by mission ownership RLS; raw storage keys and cross-user output IDs are rejected | `tests/preview-authorization.test.ts` |
| Evidence Vault | authenticated current-model evidence ledger with extraction status, fact counts and expiring downloads | `tests/system-surfaces.spec.ts` |
| Field-image metadata privacy | JPEG and PNG evidence is orientation-normalized and re-encoded before private custody; embedded EXIF/GPS, device, ICC, XMP, and comment metadata never enters the stored or executable evidence object | `tests/mission-evidence.test.ts` |
| Executable evidence limits | Intake and executor share one 20 MB source ceiling; PDFs remain readable through 20 MB, images are privacy-normalized below Claude's 5 MB image ceiling, and any attachment that cannot be represented safely fails explicitly instead of being described to the model as conceptually considered | `tests/mission-evidence.test.ts` |
| Brand custody | create or import a user-owned brand kit; uploaded guides are hash-bound in private storage | `tests/system-surfaces.spec.ts` |
| Light and dark UI | persisted theme selection across public and protected surfaces; navigation-specific contrast tokens | `tests/system-surfaces.spec.ts` |
| Operational navigation | Archive, Telemetry, and Settings read current mission/executor state; no release-facing placeholder pages | `tests/system-surfaces.spec.ts` |
| Operational surface separation | Dashboard is a portfolio command overview with immediate reflight and environment actions; New Mission owns intake through approval and launch; Telemetry owns delivered artifacts, granular revision controls, and flight lineage. Legacy dashboard mission URLs converge without discarding mission state | `tests/system-surfaces.spec.ts` |
| Command-deck reflight | Mission Control features the most relevant mission, provides a cinematic one-click regeneration launch, and links directly to Telemetry and editable mission data without embedding the long-form engineering record in the dashboard | `tests/system-surfaces.spec.ts` |
| Evidence-grounded section planning | Required sections always remain; optional sections enter the generation plan only when backed by corresponding approved facts or attached evidence, preventing unsupported sections from being simultaneously required and forbidden | `tests/optional-section-grounding.test.ts` |
| Advanced mission launch | `/new-mission` combines natural-language intent with optional audience, deadline, format, evidence, persisted aura calibration and executable custom brand kits | `tests/system-surfaces.spec.ts` |
| Evidence-aware launch handoff | Evidence uploads advance the durable specification version and the recalculated readiness/specification state is handed intact to Mission Control | `tests/system-surfaces.spec.ts` |
| Partial evidence resilience | Multi-file intake preserves every secured file and resulting specification version when another file is rejected; rejected filenames and reasons remain visible without stranding the mission | `tests/system-surfaces.spec.ts` |
| Secure ZIP evidence intake | New Mission and active-mission evidence controls accept a field ZIP package, expand supported files into individually named evidence records, preserve archive provenance in each filename, and reject traversal paths, unsupported entries, oversized files, excessive entry counts, corrupt archives, and expansion bombs without discarding usable entries | `tests/zip-evidence.test.ts`, `tests/system-surfaces.spec.ts` |
| One authoritative intake | Retired taxonomy-first mission URLs and the legacy launch pad redirect to `/new-mission`; shell navigation links directly to the conversational intake | `tests/system-surfaces.spec.ts` |
| Stellar atmosphere | Theme-aware orange sunburst, drifting nebula and staggered star flicker; reduced-motion users receive a static background | `tests/system-surfaces.spec.ts` |
| Explicit unresolved-item consent | Mission Control disables approval while open decisions remain unless the operator explicitly accepts the exact current set; the database rejects stale, partial or additional acceptance lists and disables the legacy bypass signature | `tests/mission-control.spec.ts` plus RPC privilege verification |
| Product infrastructure separation | Legacy THEMIS/CMD rows are retained for controlled extraction, but all browser-role privileges are revoked inside APOLLO's Supabase project; no APOLLO runtime code references those tables | live catalog privilege verification |
| Product identity separation | APOLLO's built-in publication catalog contains only APOLLO and the explicitly authorized On Spot internal-pilot identity. Atlas, Habi, Metis, and Themis assets are absent from the APOLLO release tree and direct runtime lookup rejects those product slugs; external customer identities enter only through private user-owned brand kits | `tests/product-brand-separation.test.ts` |
| APOLLO operator access | The exact `support@apollomc.ai` bootstrap identity remains authorized even if hosted environment configuration drifts; configured access remains exact-email only and grants neither domain-wide nor cross-product access | `tests/auth-allowlist.test.ts` |
| Post-delivery mission edits | Editing facts reopens the New Mission workspace, clears the obsolete delivered execution from active state, requires a fresh review/approval checkpoint, and continues artifact numbering and `revision_of` lineage after reapproval | `tests/system-surfaces.spec.ts`, `tests/specification-work-order.test.ts` |
| Reflight input identity | Identical revision requests deduplicate only when the effective brand and evidence content hashes are unchanged; rotating signed retrieval URLs do not create false versions | `tests/specification-work-order.test.ts` |
| Brand-library concurrency | A late initial library response merges with locally created/uploaded kits instead of erasing the operator's new brand card | `tests/system-surfaces.spec.ts` |

## Verification snapshot — 2026-09-07

- Framework: Next.js 16.3.4 and React 19.2.8; the version-16 proxy convention is in use.
- TypeScript: passed (`npm run typecheck --workspace apps/portal`).
- Unit/contract suite: 72 passed across 20 files.
- Chromium journeys: 18 passed, including desktop/mobile mission intake, editable voice capture with critical-value review, explicit unresolved-item consent, recoverable approved and blocked execution in Mission Control and Review, evidence-aware advanced launch handoff, durable mission restore without browser cache, legacy-route convergence, Evidence Vault, brand custody, operational surfaces, both themes, atmospheric rendering, reduced motion and controlled review/revision.
- Production build: passed with all portal, executor and Workflow routes emitted.
- Dependency audit: 0 critical, 0 low, 2 moderate and 14 high. All 16 remaining advisories originate in `workflow@4.8.5` and its pinned `nanoid`/`undici` graph. npm's proposed forced remediation is a breaking downgrade to Workflow 2.0.6, so it is not an acceptable automatic release change. Track the upstream 4.x remediation before public release.
- Billing remains scaffold-only. `BILLING_MODE=internal` is a release invariant and no payment provider is activated.

## Verification snapshot — 2026-09-14

- Release-candidate commits `eb38f02` and `6aeaba3` separate command overview, mission authoring, and telemetry/reflight responsibilities and preserve approved edit lineage.
- TypeScript passed with the current Next.js 16.3.4 application.
- Unit and contract suite passed: 107 tests across 22 files.
- Chromium acceptance passed in one combined run: 23 journeys, including command-surface separation, legacy-link convergence, telemetry-owned reflight controls, post-delivery factual editing with a fresh approval checkpoint, brand-kit concurrency, mobile layout, both themes, evidence handoff, failure recovery, and immutable revision history.
- Production build passed with all 29 application pages and the complete executor/API surface emitted.
- Vercel deployments `dpl_HACoiTXNjTSDMfnsfnisCMg8ZfvR` and `dpl_3fXhVBCWL7jUSQK6Jzp9sduYSz5K` reached `READY`; the stable release-candidate alias was assigned to each successive commit.
- Deterministic agreement, federal identity, field-record, commercial, PWA fallback, and terminal-email routing gates increased the unit/contract suite to 125 passing tests across 27 files. Daily construction and final-QC publication now fails closed if approved identity, crew/activity/status rows, acceptance criteria, or measured results drift during generation. Quotes and invoices protect every approved line-item row; change orders also verify the resulting contract-sum arithmetic. A repeated evidence-handoff race was caught by the combined browser gate and removed by synchronously retaining the queued file batch; the focused journey passed five consecutive runs and the complete 23-journey Chromium suite then passed together. Deployments `dpl_8i5Cu88Xi7iRsZYcRkgkq8xjpKPi`, `dpl_DCCxujDig4qmH2SaMzioDxSEj1De`, and `dpl_DF4mBUDveP76sZC1pshkhRcuUZ9G` reached `READY` for the preceding gates and received the stable release-candidate alias.
- Evidence custody now canonicalizes blank or generic browser MIME declarations before execution. DOCX and XLSX remain immutable in original storage while execution receives a separately hashed UTF-8 derivative; PDFs, images, CSV, and text retain their normalized direct-retrieval MIME. A real generated XLSX workbook proves sheet names and financial rows survive extraction. The unit/contract suite is now 128 passing tests across 27 files, TypeScript passes, and the 29-page production build succeeds.
- Conversational intake and the executable catalog now agree: all 37 active deliverables are reachable by their canonical names, including formerly shadowed exact requests such as Cash Flow Forecast, Incident Report, Invoice, and Meeting Minutes. Catalog modules retain their domain playbooks, so financial, field, government, legal, and executive controls remain active. A catalog-wide gate verifies unique registration, module/schema presence, conversational routing, complete required-fact locking, and work-order contract validity for every deliverable. The full unit/contract suite now passes 203 tests across 28 files; all 23 Chromium journeys and the complete production build also pass.
- Mission Control is restored as the command deck: it features the current mission, provides a large countdown reflight control, and exposes direct Telemetry and Edit Mission Data actions while keeping the detailed engineering record inside those environments. Optional document sections are now activated only by approved supporting facts or evidence. The full suite passes 207 unit/contract tests across 29 files and 24 Chromium journeys; TypeScript and the complete production build pass. Vercel deployment `dpl_26bg9CyV5E8bFBoEyatQEk5Y4W4B` reached `READY` for commit `4e749de`, received the stable branch alias, and the post-deployment runtime error scan was empty.
- Field-photo custody now strips embedded metadata before storage or execution. JPEG and PNG evidence is decoded, orientation-normalized, and re-encoded with a bounded pixel limit; malformed or unsafe images fail closed. The privacy fixture proves EXIF removal and correct visual orientation rather than merely searching raw bytes for metadata tags.
- The complete 37-deliverable production-renderer matrix exposed blank custom-brand mastheads in nine formats (capability statements, invoices, one-pagers, FSRs, incident reports, meeting minutes, daily construction reports, completion notices, and toolbox talks). The shared render identity fallback now uses the selected custom kit label across every layout. All 37 formats pass client-identity isolation, required-section, controlled-HTML, and packet-cover invariants.
- Mixed-evidence limit testing found a no-drift defect: intake accepted individual files through 20 MB, but execution rejected sources above 10 MB and the orchestrator silently replaced PDF/image evidence above 5 MB with a claim that it had been considered. The custody and executor now share the same 20 MB ceiling, PDFs remain executable through that ceiling, normalized images are constrained to the provider's 5 MB image limit, and unreadable evidence fails explicitly. No execution prompt may claim an attachment was used when its content was unavailable.
- Conflict-flow testing found that an operator could answer a question created by contradictory evidence yet remain trapped in the same conflict. Direct user statements now serve as the explicit adjudication authority for an active evidence conflict; the selected value becomes a stated fact and clears the blocking candidate set. Any later source that contradicts that choice reopens the conflict, preserving truthfulness rather than permanently suppressing disagreement.
- Billing remains internal and dormant. No payment activation is part of this pilot gate.
- Voice turns now preserve input channel, transcription confidence, and critical-value confirmation in owner-scoped conversation records. Both the API and PostgreSQL reject unconfirmed critical voice values, and restored transcripts display the retained provenance badge.
- The production build environment contract now declares every configured APOLLO executor and Drive variable; deployment builds complete without Turborepo environment-declaration warnings.
- All eight finance/accounting deliverables now pass through deterministic source-basis verification instead of six formats failing as unsupported. The financial suite passes 12 cases and the full unit/contract suite passes 257 tests.
- Strict product separation now extends through publication identity and hosted configuration. Atlas, Habi, Metis, and Themis are no longer built-in APOLLO brands or bundled release assets. Three legacy METIS executor variables, the unused APOLLO callback URL, and the obsolete deployment-wide Google Drive refresh token/root folder were removed from Vercel; APOLLO retains only its client credentials and resolves encrypted user-scoped Drive custody at execution time.
- Field-package intake now accepts ZIP files through the same drag/drop and picker surfaces used for ordinary evidence. APOLLO expands supported entries locally for individual custody, carries the archive name into each evidence filename, reports rejected entries, and prevents launch while package inspection is active. ZIP-specific security gates cover path traversal, per-entry and aggregate expansion limits, entry-count limits, CRC validation, corruption, and unsupported nested content.
- Live terminal-notification audit found both missions delivered on 2026-09-14 recorded `completion_email_status=sent`; older historical runs retain migration-default pending states. APOLLO now adds a second idempotent recovery path for terminal alerts created on or after 2026-09-14: a CRON-secret-protected ten-minute pass plus an owner-scoped post-dashboard reconciliation. The historical backlog is deliberately excluded so enabling recovery cannot emit an unexpected blast of old pilot emails.
- Live golden-generation testing now covers seven representative output archetypes through the real Claude orchestration and PDF renderer without customer data or external custody side effects. Contract Package, Proposal, Federal Proposal, Cash Flow Budget Package, Pitch Deck, Final QC Report, and Capability Statement all passed their deterministic workmanship gates; the representative scores after repair were 92–100, and a dedicated Executive Presentation proof also passed at 100.
- Presentation-class deliverables no longer inherit the portrait contract renderer. Pitch Deck and Executive Presentation now use a true 16:9, 960 × 540-point publication canvas with a branded title slide, one decision-led section per slide, slide numbering, adaptive density, and no report TOC, Roman section furniture, or signature page. Their generation prompt requires concise live-presentation composition, and the workmanship gate requires real decision tables plus slide-native lists rather than accepting pipe-delimited prose. Final visual inspection proved an 11-slide Pitch Deck and 10-slide Executive Presentation with no overflow or continuation-page defect.
- Contract Package regeneration reached 100 and visual inspection confirmed exactly one renderer-owned execution page; the model-authored duplicate bare signature section is now suppressed while substantive acceptance prose remains available to formats that require it.
- A second live golden batch expanded pilot coverage to Invoice, Change Order, Statement of Work, Daily Construction Report, Meeting Minutes, and Tax Estimate. All six passed the deterministic publication floor (96–100). Visual review found and corrected duplicate model-authored mastheads in invoice, minutes, tax, and change-order layouts; Invoice contracted from four pages to a two-page AP-native document, Meeting Minutes and Tax Estimate each contracted by one page, and Change Order now ends with a deterministic client/provider execution page. Definition-list styling preserves readable label/value separation in invoice and financial forms.
- A third live risk-archetype batch covers Quote, Field Service Report, Incident Report, NDA, Compliance Report, and Board Report. The first visual pass rejected technically complete but operationally excessive 10-page FSR and 18-page near-miss outputs. FSR and Incident Report now use a compact field-native renderer and 15–70-word section contract, yielding visually verified two- and three-page records with deterministic technician/customer or reporter signature lines. Quote no longer duplicates its renderer-owned masthead. The structured-output boundary now names exact missing, duplicate, and misordered section keys, while safely discarding non-approved extra sections instead of either publishing scope drift or failing an otherwise complete mission. All six classes ultimately passed live generation at 96–100.
- A fourth live publication batch covers Financial Statements Package, Expense Report, Personal Monthly Summary, One-Pager, Project Completion Notice, and Tool Box Talk. All six passed the real Claude-to-PDF path at 100 after repair. Visual review rejected model-authored duplicate financial mastheads, a nominal one-pager spilling onto a mostly blank second sheet, expense/personal reports padded to four pages, and field acknowledgments stranded alone on page two. Layout-owned mastheads are now excluded from the model contract; the true one-page commercial layout uses a balanced two-column editorial grid and client/company subject title; compact accounting and closeout treatments reduce the verified outputs to 4, 2, 3, 1, 1, and 1 pages respectively without removing approved facts.
- The post-change verification baseline is 280 passing unit/contract tests across 33 files, 26 passing Chromium journeys, a clean TypeScript check, and a successful 29-page production build.
- Strategic-publication live generation exposed two integrity defects that nominal scores had missed: long-form structured output could exhaust its response budget, and generic model knowledge could introduce unsupported market figures or remembered legal citations. Token budgets now scale with the active section contract; every repair retains the original fields and evidence; and deterministic source-boundary checks reject unsupported commercial figures and legal authorities. Visually inspected Business Plan, Market Analysis, Investor Memo, Investor Update, Audit Readiness, and Legal Memo outputs now pass at 96–100 with decision tables, compact editorial pagination, explicit validation gaps, and no recalled case authority.
- Contract Intelligence Review is now the 38th active specialist deliverable. Natural-language requests to review a contract, agreement, or warranty route to operational analysis instead of contract drafting. Its evidence-first structure covers current status, critical dates, plain-English clause mapping, party obligations, costs and coverage, commonly missed restrictions, value-capture actions, professional escalation, and a clause-linked action calendar. Intake requires the agreement rather than a questionnaire, long extracted documents are processed in overlapping segments up to the bounded custody limit, and publication quality fails closed without substantial tables, operational status labels, and visible clause/page anchors. The real Claude-to-PDF pilot initially failed closed on an invalid keyed-section shape, then rejected a 4,120-word draft because it introduced two unsupported dollar figures. Deterministic section normalization and an explicit permitted-figure ledger resolved those defects; the final synthetic warranty review passed at 96 with 11 tables, 114 rows, only source-approved financial figures, no invented authority, and a visually inspected 25-page navigable packet.
- A live Field Service mission exposed a deployment-contract defect at the deterministic verification boundary: the workflow wrote four specialist verification records that had not been added to `apollo_document_jobs`. Two preserved attempts failed safely at 25%, and both failure notifications were sent. Migration `20260915221422_add_document_job_verification_records.sql` adds the four non-null JSON-object records with database constraints and is recorded in APOLLO Supabase migration history. A schema-contract regression test now proves every verification field written by the workflow is represented in the migration.
- Mission Control retains its command-station character without absorbing the granular mission editor: the latest successful mission owns the large cinematic reflight control and immediately visible mission quick actions, while editing and detailed telemetry remain linked operating environments. The current full baseline passes 288 unit/contract tests across 33 files, 26 Chromium journeys, TypeScript, and the production build.

## Verification snapshot — 2026-09-16

- Regeneration authority is no longer free-form: revision directives are hash-bound and presentation-only, while the approved specification remains authoritative for mission identity, factual content, evidence, commercial and financial figures, and branding.
- Specialist integrity now crosses the render boundary. APOLLO parses the exact final PDF, reruns the applicable field, commercial, financial, agreement, and federal checks against that extracted text, and records its normalized factual-content digest in the artifact manifest.
- Controlled artifact pickup now verifies both immutable Drive bytes and, for upgraded manifests, reparsed factual content before returning a PDF through APOLLO-owned access. Existing legacy artifacts remain retrievable under their prior byte-integrity contract.
- The current local baseline is 398 passing unit/contract tests across 46 files, a clean TypeScript check, and a successful Next.js 16.3.4 production build.
- Commit `ffe63cd` deployed as `dpl_6ti1Np7GfHuo9mVD8i3a6RPCoy8N`; Vercel reported `READY`, assigned the stable release-candidate alias, and returned no runtime error entries in the post-deployment scan.
- This is hardened infrastructure evidence, not six-class completion evidence. Pilot release remains open until fresh live missions for FSR, Final QC, market-informed Quote, Proposal, Financial Packet, and Contract Review each produce an upgraded manifest and pass intake, autonomy, launch, output, telemetry, recovery, and regeneration gates.

## Hosted evidence

- GitHub branch `audit/conversational-intake-rebuild` is the tested release candidate.
- GitHub Actions run `34043939894` passed the production build, lint baseline, typecheck, 48-test unit/contract suite, Chromium installation, and the complete browser acceptance suite for commit `d3e38b4`.
- The Vercel preview for release-candidate commit `b6db13e` reached `READY`. Its homepage and login returned HTTP 200, and a protected dashboard request safely resolved to the explicit configuration boundary with HTTP 200 rather than a middleware/render failure. Deployment-scoped runtime logs contained no error or fatal entries after these probes.
- Preview deployment `dpl_35YHycDwzYTxhwLj4D6N6VofW4Qa` for commit `01b8906` reached `READY`. Its build log contains no Turborepo environment-declaration warnings after the application environment allowlist was added to `turbo.json`, and the obsolete Active CPU memory override was removed.
- The stable preview is fully configured for the APOLLO Supabase project and authenticated mission-control use. The prior hosted-configuration boundary is resolved.
- The APOLLO Supabase project `yarbyhyomuimetsppsrz` is now connected and healthy. The conversational mission-control and brand-kit migrations were applied on 2026-09-06. Live catalog verification proves RLS is enabled on all five new tables with the expected 15 ownership policies.
- A live rollback transaction exercised authenticated mission creation, specification approval, cross-user rejection and the server-only rate limiter without retaining test data.
- The approval migration hashes the exact approved JSON and does not append evidence facts a second time. `pgcrypto`, caller-privilege execution, Data API grants, least-privilege function grants, ownership RLS and foreign-key indexes are asserted in migration/pgTAP evidence.
- `20260906154531_harden_legacy_database_functions.sql` fixes mutable search paths and removes anonymous/authenticated access to privileged trigger and rate-limit functions. The live security advisor now reports no exposed-function findings; remaining no-policy notices are informational for deliberately server-only tables.
- Live internal mission `ad64eff1-3a37-5a8d-a8af-3c6dca5f6b97` proved the complete intake, 29-fact specification, zero-open-decision approval, durable generation, schema validation, and corrected serverless PDF rendering path on commit `fdf061f`. Execution then reached the external Drive boundary and Google returned `invalid_grant` for the legacy refresh credential.
- Commit `c94d022` adds user-scoped Drive reconnection from Settings and the private `apollo_google_drive_connections` table. Tokens are encrypted at rest and are never returned to the browser.
- The September 7 Drive blocker is resolved. Live verification on 2026-09-14 found one active APOLLO-scoped Drive connection with a dedicated custody folder, 11 delivered jobs with 11 artifact manifests, five delivered version-2-or-later artifacts, five revision jobs, and ten retry-lineage jobs. This proves hosted delivery, revision, regeneration, and retry custody without substituting another product's connection.
- Commits `9bebae6`, `6ec59b9`, `1e3dd70` and `15a8e39` add complete semantic provenance, a guarded execution lifecycle, non-destructive isolation of 14 legacy non-APOLLO tables, and exact unresolved-item consent. The matching migrations are applied to APOLLO Supabase.
- Vercel deployment `dpl_5mM2roQXazHmuEk6WNLGF8F993UT` reached `READY` for exact commit `15a8e390e958cd6682c2603a86fb82afebd5b9ea`; the stable branch alias reported that same service version and a 30-minute runtime scan returned no errors.
- Vercel deployment `dpl_AZFHLAzqrAwyAJ98Fr3YdbNHC4Y9` reached `READY` for exact commit `18973781ef47d0a9c9f5577b8c165e019c4a1c9a`; the stable branch alias reported the same deployment and the one-hour runtime error scan was empty. Commits through this point converge all mission-entry URLs, remove the unreferenced taxonomy-first React tree, align the browser fixture with production provenance, internalize the APOLLO logo, stabilize legacy realtime subscriptions and tighten current evidence/session typing.
- Vercel deployment `dpl_3KN5zpRZbs2J1RX1Xki9JeCsSfnM` reached `READY` for exact commit `f20c45202d64492312a56e9c6380f7d3c3bd6aff`, and the stable preview alias resolved to that deployment. This release authorizes only the exact APOLLO bootstrap identity, adds mission-preserving Drive reconnection, and returned no 4xx runtime requests in the post-deployment verification window.

## Release commands

From the repository root:

```text
npm run typecheck --workspace apps/portal
npm test --workspace apps/portal -- --run
npm run build --workspace apps/portal
cd apps/portal && npx playwright test --project=chromium
npm run release:check --workspace apps/portal
```

The first four gates can run locally. The environment check must run inside the hosted service so it can validate presence without copying or printing secret values.

## Hosted activation

Keep `BILLING_MODE=internal`. In the existing Google OAuth client's authorized redirect URIs, register:

```text
https://portal-git-audit-conversational-i-1da0d5-support-3556s-projects.vercel.app/api/integrations/google-drive/callback
```

Then use Settings → Connect Google Drive once and execute the existing internal golden mission through Drive custody and revision before promotion. Never substitute another Supabase project.
## Concurrent evidence custody

- Evidence specification commits now use optimistic concurrency against the mission's current specification version.
- If simultaneous uploads race, the losing request rereads the newest specification and rebases its accepted evidence instead of replacing another file's custody record.
- The database commit remains `security invoker`, verifies mission ownership, locks the conversation row, rejects stale expected versions with a serialization error, and is executable only by `authenticated`.
- Verification: 250 unit tests across 30 files, TypeScript, and the production build pass; a focused rebase test proves both evidence sources remain in the latest specification.
- The complete Chromium acceptance suite also passes 24/24 journeys against this code state, covering command-deck presentation, advanced intake, evidence handoff, explicit consent, autonomy, failure recovery and notification requests, Drive-blocked recovery, review revision, version lineage, operational navigation, responsive layout, and both themes.
- Vercel deployment `dpl_Bg4GhJprafsRWzaohswpZJmruCBx` reached `READY` for commit `3e36acf`, the stable branch alias served that exact deployment, and the post-deployment runtime error scan was empty.
- A fresh production dependency audit found and removed the new direct `sharp<0.35.4` libheif advisory. APOLLO now pins `sharp@0.35.4`; field-image metadata sanitization, all 250 unit/contract tests, TypeScript, and the production build pass. The only remaining production advisories are the 16 transitive Workflow DevKit findings described above; the available npm remediation would incorrectly replace Workflow 4.8.5 with the incompatible 2.0.6 line.
- Supabase migration `20260914213441_preserve_voice_turn_metadata.sql` is applied to project `yarbyhyomuimetsppsrz`. Live verification confirms all four metadata columns, the confirmation constraint, caller-privilege execution, empty search path, no anonymous execution, and authenticated execution.
- Vercel deployment `dpl_4b17erwTgWM7v9VpYJXepqWWH8aX` reached `READY` for commit `d691748`; the stable alias reported that exact service revision, all 37 deliverables, healthy status, and no runtime errors in the post-deployment scan.

## 2026-09-17 six-class engine and live-gate audit

- Commit `8c1f4f5` binds dashboard and telemetry current state to the exact approved specification ID, hash, and deliverable type. Historical flights remain visible but are labeled as prior-specification records and cannot control current mission actions.
- Commit `1726409` adds bounded schema recovery plus deterministic normalization for nested, keyed, and JSON-encoded model section payloads. This closed live model-shape failures in Quote and Contract Review without weakening schema validation.
- The controlled generation matrix passed all six release classes against the real model and PDF engine: FSR 96, Final QC 100, Quote 100, Proposal 100, Cash Flow Budget Package 96, and Contract Intelligence Review 96.
- The release gate passed 407 tests across 48 files, 23 Chromium system journeys, TypeScript, and the Next.js production build. Deployments `dpl_7RDFUHpZwPJjbSPqzaxuYa5Zvnhw` and `dpl_69f8Jm7s8gR951HsNUDRDWUqMjBV` reached `READY` on the stable preview alias, and both post-deployment error scans were empty.
- `BILLING_MODE=internal` is explicitly configured in both Preview and Production. Billing activation remains deferred.
- A live APOLLO Supabase read on 2026-09-17 found 11 missions. Current specifications include live FSR, Final QC, Quote, Proposal, and Cash Flow Budget Package candidates, but no Contract Intelligence Review mission. The newest Quote and Final QC candidates were `ready`, not approved. Therefore the live six-class pilot gate remains open even though all six generation archetypes pass.
- Commit `f7eede0` makes pilot representative selection deterministic by immutable mission `created_at` plus an ID tie-breaker. It no longer uses mutable `updated_at`, which had been equalized by a backfill and could select an arbitrary draft as the class authority.
- Commits `4882d27` and `b0854e1` add the evidence-first six-class gate and the systemic defects it exposed: canonical select-option normalization across legacy and current catalog shapes, same-source partial-scalar promotion, verified row-collection composition for QC, quote, and financial fields, structured numeric quote-line recognition, and deterministic recovery of complementary model section attempts.
- Each release class passed the controlled evidence-first path against the real extraction, calibration, generation, verification, and PDF engines with a completed multipass trace and zero remaining execution gaps: FSR 96, Final QC 100, Quote 100, Proposal 100, Cash Flow Budget Package 96, and Contract Intelligence Review 96.
- The post-change local baseline is 413 passing unit/contract tests across 48 files, a clean TypeScript check, a clean diff-integrity check, and a successful Next.js 16.3.4 production build. This proves the controlled engine matrix; the separate live hosted six-mission gate remains open until representative approved missions also prove delivery, telemetry, recovery, and regeneration under production custody.
- The exact release commit `f7229c968607be189836e61af152c76ce07960f2` deployed as `dpl_46e1f7YTMhMTbkjRQbJnQ4x68R4K`, reached `READY`, received the stable release-candidate alias, returned HTTP 200 through both deployment and stable aliases, and produced no error or fatal runtime entries in the post-deployment scan.
- The repeatable `pilot:live-audit` operator command runs the same twelve-gate production auditor without browser-cookie dependence and refuses ambiguous multi-owner selection. Its first hosted read window on 2026-09-17 was prevented by an upstream Supabase origin outage: Management SQL timed out twice while project metadata remained `ACTIVE_HEALTHY`, then direct Auth and Data API paths returned 504/522. No database state changed. The hosted six-mission gate therefore remains unproven rather than being reported as failed or passed.

## 2026-09-17 twenty-pass mission-operations regression audit

1. Runtime logs: repeated intake `409` responses were traced to recalibration attempts on an archived FSR draft with no verified sources; regeneration itself was not running.
2. Workflow state: the affected archived draft had evidence but no execution jobs, while the approved Encore FSR retained five delivered jobs.
3. Deployment authority: the release-candidate branch and stable Vercel project remain the only deployment target for this repair.
4. Mission inventory: generic duplicate Encore missions and the valid quote were identified without mutating or deleting production records.
5. Duplicate containment: archived missions with no job history are hidden from operational views while remaining preserved in Archive.
6. Dashboard completeness: the five-row truncation was removed, so a valid quote cannot disappear merely because newer drafts exist.
7. Historical authority: execution rows now resolve the exact specification version that created each job instead of inheriting the mission's newest specification.
8. Mission identity: derived titles combine site, customer, or project context with the deliverable, such as `Encore Boston Harbor · Field Service Report`.
9. Flight identity: every execution receives a descriptive launch or reflight name rather than an isolated sequence number.
10. Revision purpose: telemetry exposes the revision directive or launch purpose so operators can distinguish reflight outcomes at a glance.
11. Explicit reflight idempotency: a client request ID makes one submission retry-safe while allowing a later deliberate reflight with identical wording to create a new job.
12. Concurrent launch protection: an already-active mission job is returned instead of creating a duplicate execution loop.
13. Evidence continuity: regeneration remains bound to the mission and approved specification lineage; no replacement intake or evidence reattachment is required.
14. Failure recovery: failed or blocked flights can be retried directly from telemetry rather than routing through conversational intake.
15. Telemetry freshness: mission activity and ordering now follow the newest execution timestamp, including reflight activity.
16. Metric integrity: dead archived drafts do not dilute mission totals or readiness averages, while active historical-spec jobs remain visible.
17. Operational ordering: mission rows are ordered by genuine activity and retain complete immutable flight history.
18. Interface clarity: dashboard and telemetry search, context lines, timestamps, short job IDs, failure labels, and recovery actions use the same mission identity contract.
19. React and accessibility regression: the changed surfaces pass the repository's type, component, and interaction checks without introducing a parallel mission-control interface.
20. Full verification: unit and contract tests, production build, and Chromium system journeys cover quote visibility beyond five missions, archived-draft containment, descriptive reflight lineage, regeneration, and recovery.
